---
---
{
    "posts": [
        {% for post in site.posts %}
            {
                "title": "{{ post.title }}",
                "tags": [
                    {% for tag in post.tags %}
                        {% if tag == post.tags|first %}
                            "{{ tag }}"
                        {% else %}
                            ,"{{ tag }}"
                        {% endif %}
                    {% endfor %}
                ],
                "href": "{{ post.url }}",
            },
        {% endfor %}
    ],
}
